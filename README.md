# 스파이를 찾아라! v29

## 긴급 안정화 수정
- 구형/일부 태블릿 브라우저에서 지원되지 않을 수 있는 canvas `roundRect()` 의존성 제거
- 직접 그리는 둥근 사각형 함수로 교체하여 화면 렌더링 중단 방지
- 게임 상태(state)에 NPC 목록을 포함해 접속/재접속 시 즉시 동기화
- 게임 시작 팀 공개 단계에서도 NPC 상태를 모든 학생에게 즉시 전송
- 렌더링 오류가 한 번 발생해도 requestAnimationFrame 전체가 영구 중단되지 않도록 보호
- index/game.js/style.css 캐시를 끄고 v29 쿼리스트링을 추가해 태블릿이 예전 JS를 사용하는 문제 방지
- 기존 v28/v27 기능 유지

- 학생 클라이언트가 2초마다 서버의 현재 phase/players/NPC 상태를 재동기화해 이벤트 유실 시 자동 복구
- 태블릿이 화면 잠금/탭 전환 후 돌아올 때 즉시 상태 재요청
